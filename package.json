{
  "name": "avocado-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for Vue frontend and Nest backend",
  "scripts": {
    "format": "prettier --write \"**/*.{ts,js,vue,json,md}\"",
    "lint": "eslint ."
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "mammoth": "^1.11.0"
  }
}
